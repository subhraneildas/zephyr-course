if(CONFIG_L6_DRIVER)
    target_sources(app PRIVATE l6driver.c)
    #target_include_directories(app PUBLIC ${CMAKE_CURRENT_SOURCE_DIR})
    if(CONFIG_SHELL)
        target_sources(app PRIVATE l6driver_shell.c)
    endif()
endif()
